# 📓 Bitácora — Agosto 2026

> [!NOTE]
> **Entradas más recientes arriba.** Copia el bloque de [`_PLANTILLA_dia.md`](./_PLANTILLA_dia.md) para cada día nuevo.

## 📅 2026-08-10 — Ejecución Semana 5 Día 1 (`R2-XUB-002` - Índice BWA & `MVP-002/003` - API Tm Biopython)

- **⏱️ Tiempo:** 5.5 h (Descarga e indexación BWT del genoma de referencia y endpoint FastAPI TDD).
- **🎯 Objetivo del día:** Preparar el terreno para NGS con el índice de *E. coli* e iterar el microservicio para exponer lógica bioinformática de negocio (`calculate_tm`).
- **🛠️ Qué hice:** (1) Instalado `bwa` y generado archivos `.amb, .ann, .bwt, .pac, .sa` para búsqueda rápida sin colapsar RAM. Gestionado error de `inter-device move` haciendo cuarentena segura. (2) Ciclo Red-Green-Refactor en `asistente-estadistica-tesis`: creada prueba en `tests/test_tm.py`, implementado `Tm_Wallace` en Biopython (`src/services/tm_calculator.py`). (3) Creada prueba de endpoint `test_api.py` (404 a 200 OK) y expuesta ruta `/tm` en `FastAPI`.
- **💻 Comandos Útiles del Día:**
  ```bash
  # 1. Crear el FM-index para alineamiento ultra-rápido en NGS
  bwa index ecoli_ref.fasta

  # 2. Copiar directorios completos entre sistemas Windows y ext4(WSL)
  cp -r /mnt/c/Users/BullTech/data/reference/* data/reference/

  # 3. Lanzar suite completa de pruebas Python de extremo a extremo
  PYTHONPATH=. pytest tests/ -v
  ```
- **📚 Qué aprendí:**
  1. **BWA-MEM y BWT:** El genoma de referencia en texto puro (FASTA) no sirve para mapeo. Es obligatorio transformarlo matemáticamente en un FM-index para que la búsqueda de millones de lecturas sea viable en memoria.
  2. **El Síndrome de la Complejidad:** El código matemático es simple pero la arquitectura web es robusta. El usuario no paga por la complejidad de la fórmula, paga por la fricción eliminada.
  3. **Rigor sobre Borrados:** Las equivocaciones de rutas cruzadas no justifican romper reglas de borrado; la "basura" se archiva para trazabilidad.
- **🔗 Evidencia:** Índice `bwa` funcional. Suite Pytest en verde (`100% PASSED`). Commits quirúrgicos ejecutados.
- **➡️ Siguiente paso:** Arrancar Semana 5 Día 2 (`R2-XUB-003` - Alineamiento BWA: interpretar tasa de mapeo).

---

## 📅 2026-08-10 — Ejecución Semana 4 Día 3 (`R2-XUB-001` - Trimming NGS con `fastp` & `MVP-001` - Microservicio FastAPI)

- **⏱️ Tiempo:** 5.0 h (Trimming y QC NGS en `fastp`, resolución de dependencias `scipy`/`statsmodels` y despliegue API FastAPI)
- **🎯 Objetivo del día:** Transicionar al Rango II ejecutando trimming Phred Q20 de lecturas NGS con `fastp` (`R2-XUB-001`), e inicializar y verificar en vivo el microservicio REST en FastAPI (`MVP-001`).
- **🛠️ Qué hice:** (1) Instalado `fastp` v1.3.6 vía `bioconda`. Ejecutado trimming sobre `ecoli_reads.fastq.gz` filtrando lecturas con Phred $Q20$ y longitud mínima de 20 bp (`reads passed filter: 3`). Generados reportes en HTML y JSON. (2) Resuelta la excepción `ModuleNotFoundError: No module named 'scipy'` instalando `scipy` y `statsmodels` en `bio-env`. (3) Desplegado el microservicio REST en FastAPI (`asistente-estadistica-tesis`) con `uvicorn`. Verificada la respuesta HTTP `200 OK` del endpoint `/` devolviendo `{"status":"healthy","service":"Thesis Stat Assistant","version":"0.1.0"}`.
- **💻 Comandos Útiles del Día (Recetario CLI, Conda & FastAPI):**
  ```bash
  # 1. Cambiar/Activar entorno Conda en Linux/WSL
  conda activate bio-env

  # 2. Ejecutar trimming de lecturas NGS con fastp (Phred Q20, length >= 20)
  fastp -i data/raw/ecoli_reads.fastq.gz -o data/processed/ecoli_reads_clean.fastq.gz -q 20 -u 40 -l 20 -h reports/fastp_ecoli.html -j reports/fastp_ecoli.json

  # 3. Arrancar servidor Uvicorn con auto-reload
  PYTHONPATH=. uvicorn src.main:app --reload --port 8000

  # 4. Probar salud del microservicio con curl
  curl http://localhost:8000/
  ```
- **📚 Qué aprendí:**
  1. **Física del Filtrado en `fastp`:** La bandera `-q 20` establece el umbral Phred de calidad por base ($Q20 = 99\%$ precisión). La bandera `-u 40` descarta la lectura si más del 40% de sus bases no alcanzan dicho umbral. La bandera `-l 20` descarta lecturas hiper-recortadas para evitar alineamientos ambiguos.
  2. **Activación de Entornos en Conda:** El comando nativo en Bash para cambiar de entorno virtual es `conda activate <entorno>` (o `mamba activate <entorno>`).
  3. **Despliegue Asíncrono de APIs en Python:** Uvicorn utiliza el bucle de eventos asíncrono (`uvloop`/`asyncio`) para servir peticiones REST HTTP de ultra-baja latencia en FastAPI.
- **🔗 Evidencia:** Reportes `reports/fastp_ecoli.html` generados con 3 lecturas procesadas limpias; microservicio Uvicorn devolviendo `200 OK` en `http://localhost:8000/`. 720 XP acumulados. **Semana 4 COMPLETADA AL 100%.**
- **➡️ Siguiente paso:** Arrancar Semana 5 Día 1 (`R2-XUB-002` Descargar genoma de referencia e indexarlo con BWA + `MVP-002` Algoritmo de cálculo de Tm en Biopython).

---

## 📅 2026-08-10 — Ejecución Semana 4 Día 1 (`R1-BOSS-01` - El Repositorio Fantasma / Clean Architecture & Pytest)

- **⏱️ Tiempo:** 5.0 h (Auditoría hostil de portafolio, `pytest.ini`, `environment.yml` y suite de pruebas)
- **🎯 Objetivo del día:** Estructurar el proyecto bajo Clean Architecture, resolver errores de importación en `pytest` configurando `pytest.ini`, definir la reproducibilidad con `environment.yml` y ejecutar commit quirúrgico.
- **🛠️ Qué hice:** (1) Auditada la estructura modular del paquete `src/seq_stats/` y la suite `tests/test_seq_stats.py`. (2) Solucionados errores de `ModuleNotFoundError` mediante la creación de `pytest.ini` configurando `pythonpath = src .` y limitando el escaneo a `testpaths = tests`. (3) Ejecutada la suite de pruebas comprobando un resultado limpio del 100% (`4 passed in 0.36s`). (4) Creado `environment.yml` especificando canales (`conda-forge`, `bioconda`, `defaults`) y versiones clave de Python, biopython, pandas y pytest. (5) Ejecutado commit quirúrgico `feat(r1-boss): add environment.yml, pytest configuration and QC audit scripts` (`a993d83`).
- **💻 Comandos Útiles del Día (Recetario CLI & Pytest):**
  ```bash
  # 1. Configurar pytest.ini en la raíz
  cat << 'EOF' > pytest.ini
  [pytest]
  testpaths = tests
  pythonpath = src .
  python_files = test_*.py
  EOF

  # 2. Ejecutar suite de pruebas unitarias en modo verboso
  pytest -v

  # 3. Realizar commit quirúrgico por nombre explícito de archivo
  git add environment.yml pytest.ini scripts/audit_qc.py scripts/auto_qc.sh
  git commit -m "feat(r1-boss): add environment.yml, pytest configuration and QC audit scripts"
  ```
- **📚 Qué aprendí:**
  1. **Configuración Declarativa con `pytest.ini`:** Evita tener que exportar manualmente la variable de entorno `PYTHONPATH` y restringe la búsqueda de tests eliminando falsos positivos en carpetas borrador.
  2. **Reproducibilidad Declarativa en Genómica:** El archivo `environment.yml` con canalización jerárquica (`conda-forge` > `bioconda`) garantiza que cualquier evaluador pueda reconstruir el entorno en 1 comando (`mamba env create -f environment.yml`).
  3. **Higiene en Commits (`git add` quirúrgico):** Evitar `git add .` previene la inclusión no deseada de temporales o datos pesados en el repositorio de GitHub.
- **🔗 Evidencia:** Commit `a993d83`, suite `tests/test_seq_stats.py` pasando al 100% (`4 passed in 0.36s`).
- **➡️ Siguiente paso:** Ejecutar Semana 4 Día 2 (`R1-BOSS-01` Parte 2: `README.md` Senior + Simulación de clonación en limpio + Cierre formal de Rango I con +250 XP).

---

## 📅 2026-08-09 — Ejecución Semana 3 Día 3 (`R1-XUB-011` - Bash Scripting Industrial & Auditoría Q30)

- **⏱️ Tiempo:** 3.5 h (Integración de `scripts/audit_qc.py` y `scripts/auto_qc.sh` con dictamen automatizado)
- **🎯 Objetivo del día:** Robustecer el script `scripts/auto_qc.sh` conectando un módulo auditor en Python (`scripts/audit_qc.py`) para parsear el archivo `multiqc_data.json` de MultiQC v1.35, evaluar métricas de Phred Q30 y %GC, y emitir un dictamen automatizado en consola.
- **🛠️ Qué hice:** (1) Desarrollado el script `scripts/audit_qc.py` para parsear la estructura de diccionario de `report_general_stats_data` de MultiQC. (2) Integrado el paso `[3/3]` dentro de `scripts/auto_qc.sh` ejecutable con manejo de errores `set -euo pipefail`. (3) Verificada la ejecución completa sobre `data/raw` generando el dictamen `🟢 PASS (Óptimo)` y el reporte HTML.
- **💻 Comandos Útiles del Día (Recetario CLI & Bash):**
  ```bash
  # 1. Vaciar un archivo en Linux en 1 segundo antes de editarlo
  > scripts/auto_qc.sh

  # 2. Ejecutar el auditor Python independiente sobre el JSON de MultiQC
  python3 scripts/audit_qc.py scratch/auto_qc_report/multiqc_data/multiqc_data.json

  # 3. Ejecución end-to-end del pipeline automatizado de QC y auditoría
  ./scripts/auto_qc.sh data/raw scratch/auto_qc_report
  ```
- **📚 Qué aprendí:**
  1. **Auditoría Programática de Datos Clinicos:** Un pipeline industrial no depende de la inspección visual humana. Debe evaluar estructuras JSON y tomar decisiones booleanas de continuación (`PASS` / `FAIL`).
  2. **Estructura Interna de MultiQC 1.35:** `multiqc_data.json` almacena las estadísticas generales en un diccionario por módulo (`report_general_stats_data['fastqc']`).
  3. **Composición Modular de Pipeline:** Delegar la auditoría JSON a un módulo pequeño en Python invocado desde Bash combina la velocidad de orquestación de la Shell con la potencia de parseo de CPython.
- **🔗 Evidencia:** Pipeline `scripts/auto_qc.sh` probado end-to-end con dictamen `🏆 RESULTADO FINAL: TODAS LAS MUESTRAS SUPERAN EL CONTROL DE CALIDAD.`. 445 XP acumulados. **Semana 3 COMPLETADA al 100%.**
- **➡️ Siguiente paso:** Arrancar Semana 4 Día 1 (`R1-BOSS-01` ☠ El Repositorio Fantasma - Auditoría Hostil de Portafolio).

---

## 📅 2026-08-09 — Ejecución Semana 3 Día 2 (`R1-XUB-012` - Profile README + Voz IA + `auto_qc.sh`)

- **⏱️ Tiempo:** 5.5 h (Perfil de GitHub `crisiev/crisiev`, Voz IA C1 e inicio de Bash Scripting `scripts/auto_qc.sh`)
- **🎯 Objetivo del día:** Crear y desplegar el **GitHub Profile README** (`crisiev/crisiev`) con badges y métricas de portafolio, realizar simulación de voz en inglés C1 sobre métricas N50/Phred Q30, e implementar la base del script automatizado de control de calidad `scripts/auto_qc.sh`.
- **🛠️ Qué hice:** (1) Creado el repo especial `crisiev/crisiev` en GitHub y publicado un `README.md` profesional con badges de stack, tarjetas de métricas dinámicas (`github-readme-stats`) y matriz de proyectos. (2) Realizado entrenamiento oral de 15 minutos en inglés C1 recitando conceptos de N50, L50, GC content y Phred Q30 con más del 95% de precisión en reconocimiento de voz. (3) Desarrollado el script de Bash `scripts/auto_qc.sh` con manejo estricto de errores (`set -euo pipefail`), validación de argumentos, colores ANSI y ejecución automatizada de `FastQC` y `MultiQC`.
- **💻 Comandos Útiles del Día (Recetario CLI & Bash):**
  ```bash
  # 1. Otorgar permisos de ejecución Unix al script de Bash
  chmod +x scripts/auto_qc.sh

  # 2. Validar manejo de errores del script (sin parámetros)
  ./scripts/auto_qc.sh

  # 3. Ejecución del pipeline de QC en lote sobre datasets reales
  ./scripts/auto_qc.sh data/raw scratch/auto_qc_report
  ```
- **📚 Qué aprendí:**
  1. **Personal Branding en GitHub:** El Profile README (`crisiev/crisiev`) es el filtro #1 de evaluación técnica en 2026. Demostrar proyectos de producción (`seq-stats`), stack moderno y CI/CD genera diferenciación inmediata.
  2. **Defensa Rigurosa en Bash (`set -euo pipefail`):** Previene errores silenciosos y fuerza la interrupción limpia del script si un paso intermedio falla en la tubería.
  3. **Fluidez Oral Técnica en Inglés:** Recitar métricas biológicas (N50, Phred Q30, hydrogen bonds) en inglés fortalece la memoria neuromuscular para entrevistas técnicas internacionales.
- **🔗 Evidencia:** Repositorio público `crisiev/crisiev` activo en GitHub, script `scripts/auto_qc.sh` probado exitosamente con `FastQC` + `MultiQC v1.35`. 395 XP acumulados.
- **➡️ Siguiente paso:** Arrancar Semana 3 Día 3 (Completar `R1-XUB-011` con auditoría automática de métricas Q30 en el script Bash).

---

## 📅 2026-08-09 — Ejecución Semana 3 Día 1 (`R1-XUB-009` - Proyecto P1 `seq-stats`)

- **⏱️ Tiempo:** 5.0 h (Práctica deliberada en AlmaLinux WSL: Python modular, argparse CLI y pytest)
- **🎯 Objetivo del día:** Estructurar el proyecto P1 `seq-stats` como un paquete modular en Python (`src/seq_stats/`), construir la CLI (`cli.py`), escribir la suite de pruebas unitarias (`pytest`) y verificar el paso del 100% de los tests en verde.
- **🛠️ Qué hice:** (1) Construida la estructura modular `src/seq_stats/` con los submódulos `metrics.py` (GC content, reverse complement, Hamming distance), `fasta.py` (Biopython parser + N50/L50 solver) y `cli.py` (argparse con subcomandos `fasta`, `gc` y `revcomp`). (2) Creada la exportación limpia en `__init__.py`. (3) Desarrollados 4 tests unitarios en `tests/test_seq_stats.py` usando `pytest`, verificados exitosamente (`4 passed in 0.29s`).
- **💻 Comandos Útiles del Día (Recetario CLI & Python):**
  ```bash
  # 1. Probar CLI subcomando fasta en formato texto y JSON
  PYTHONPATH=src python3 -m seq_stats.cli fasta data/raw/ecoli_test.fasta
  PYTHONPATH=src python3 -m seq_stats.cli fasta data/raw/ecoli_test.fasta --json

  # 2. Probar subcomandos gc y revcomp
  PYTHONPATH=src python3 -m seq_stats.cli gc GATTACA
  PYTHONPATH=src python3 -m seq_stats.cli revcomp GATTACA

  # 3. Ejecutar suite de pruebas unitarias pytest con PYTHONPATH
  PYTHONPATH=src pytest -v tests/
  ```
- **📚 Qué aprendí:**
  1. **Arquitectura Modular Clean Code:** Separar la lógica de negocio (`metrics.py`, `fasta.py`) de la interfaz de consola (`cli.py`) permite reutilizar funciones en pipelines complejos o Jupyter Notebooks sin duplicar código.
  2. **Rol de `__init__.py`:** Convierte directorios CPython en paquetes importables y controla la API pública exponiendo símbolos principales con `__all__`.
  3. **TDD / Contract-First con `pytest`:** Probar casos de prueba límites y conocidos en microsegundos asegura que futuros cambios no rompan la lógica existente.
- **🔗 Evidencia:** Mapeo de `src/seq_stats/`, suite `tests/test_seq_stats.py` con resultado `4 passed in 0.29s`. 370 XP acumulados.
- **➡️ Siguiente paso:** Arrancar Semana 3 Día 2 (`R1-XUB-012` GitHub Profile README `crisiev/crisiev` + Voz IA Inglés + inicio `R1-XUB-011`).

---

## 📅 2026-08-07 — Ejecución Semana 2 Día 2 (`R1-XUB-001`) & Día 3 (`R1-XUB-007`)

- **⏱️ Tiempo:** 10.25 h (Práctica deliberada en AlmaLinux WSL: FastQC/MultiQC + pandas)
- **🎯 Objetivo del día:** Control de calidad de secuencias FASTQ (`R1-XUB-001`), toma de decisiones bioinformáticas Phred $Q30$, y manipulación/limpieza de tablas de metadatos genómicos con `pandas` (`R1-XUB-007`).
- **🛠️ Qué hice:** (1) Instalados `fastqc` y `multiqc` en `bio-env`. Generados reportes en `scratch/qc_test/` y auditadas secuencias sobre-representadas. (2) Construido el script Python `scratch/pandas_metadata/clean_metadata.py` para estandarizar texto (`.str.lower()`), filtrar filas nulas (`dropna`), aplicar cobertura mínima ($\ge 1,000,000$ lecturas) y calcular agrupaciones estadisticas (`groupby` y `value_counts`).
- **💻 Comandos Útiles del Día (Recetario CLI & Python):**
  ```bash
  # 1. FastQC y MultiQC
  fastqc sample_control.fastq.gz -o . && multiqc .

  # 2. Script Python pandas para metadatos
  python clean_metadata.py
  ```
  ```python
  # 3. Métodos clave de pandas
  df['condition'] = df['condition'].str.lower()
  df = df.dropna(subset=['reads_count'])
  df = df[df['reads_count'] >= 1000000]
  df.groupby('condition')['reads_count'].mean()
  ```
- **📚 Qué aprendí:**
  1. **Física Phred:** $Q = -10 \log_{10}(P)$. $Q30$ representa 99.9% de precisión. La representación Phred+33 mapea calidad a un carácter ASCII único.
  2. **Limpieza de DataFrames:** La consistencia de cadenas (`str.lower()`) y la imputación/eliminación de nulos son prerrequisitos obligatorios antes de alimentar cualquier pipeline de genómica o RNA-seq.
  3. **Estadísticas de Grupo:** `groupby` en pandas permite comparar métricas clínicas entre réplicas y condiciones (control vs tumor).
- **🩹 Cicatrices (errores):** Intentar ejecutar `git add` fuera de la raíz `~/empleoBio`. Corregido regresando con `cd ~/empleoBio`.
- **🔗 Evidencia:** `scratch/qc_test/multiqc_report.html` y `scratch/pandas_metadata/samplesheet.csv`. 295 XP acumulados.
- **➡️ Siguiente paso:** Arrancar Semana 3 Día 1 (`R1-XUB-008` Git & SSH keys + `R1-XUB-009` GitHub P1 + README).

---

## 📅 2026-08-06 — Ejecución Semana 2 Día 1 (`R1-ROSA-03` & `R1-XUB-006`)

- **⏱️ Tiempo:** 6.0 h (Práctica deliberada en AlmaLinux WSL + Python)
- **🎯 Objetivo del día:** Algoritmos de traducción de ARN a proteína, detección de motivos de ADN (Rosalind) y desarrollo del parser FASTA para cálculo de estadísticas de ensamblaje (N50 y L50).
- **🛠️ Qué hice:** (1) Implementada función de traducción ARN $\rightarrow$ Proteína manejando codones de parada (`Stop '*'`) y mapa de tabla genética. (2) Desarrollado algoritmo `find_motif` para encontrar posiciones 1-indexed de patrones en secuencias de ADN. (3) Construido script Python `fasta_n50.py` para parsear archivos FASTA grandes, acumular sumas de contigs, ordenar longitudes en orden descendente y calcular métricas N50 (longitud de contig donde se cubre el 50% del tamaño total) y L50 (conteo mínimo de contigs requeridos para alcanzar el N50).
- **💻 Comandos Útiles del Día (Recetario Python & CLI):**
  ```python
  # 1. Parsing de FASTA y cálculo de N50/L50
  from Bio import SeqIO
  lengths = sorted([len(record.seq) for record in SeqIO.parse("assembly.fasta", "fasta")], reverse=True)
  half_genome = sum(lengths) / 2
  cum_sum = 0
  for l50, l in enumerate(lengths, 1):
      cum_sum += l
      if cum_sum >= half_genome:
          n50 = l
          break
  print(f"N50: {n50}, L50: {l50}")
  ```
- **📚 Qué aprendí:**
  1. **Definición Biológica de N50 vs L50:** N50 es una métrica de longitud en pares de bases (bp); L50 es un conteo (número ordinal de contigs). Un N50 alto y un L50 bajo indican un ensamblaje genómico continuo y de alta calidad.
  2. **Coordenadas 1-indexed vs 0-indexed:** En formatos genómicos (VCF, BAM, Rosalind), las posiciones se representan empezando en 1, mientras que en Python son 0-indexed. Se requiere sumar $+1$ al exportar o reportar posiciones biológicas.
- **🔗 Evidencia:** Scripts `rosalind_warmup.py` y `fasta_n50.py` en `scratch/` verificados en AlmaLinux WSL. 195 XP acumulados.
- **➡️ Siguiente paso:** Arrancar Semana 2 Día 2 (`R1-XUB-001` QC FASTQ).

---

## 📅 2026-08-04 — Ejecución Semana 1 Día 3 (`R1-ROSA-01`, `R1-ROSA-02`, `R1-XUB-010` & Voz IA)

- **⏱️ Tiempo:** 5.0 h (Práctica deliberada en AlmaLinux WSL)
- **🎯 Objetivo del día:** Algoritmos de secuencias en Python (transcripción, complemento reverso, contenido GC y distancia Hamming), simulación de voz en inglés C1 y hardening del sistema de archivos Unix con `chmod`.
- **🛠️ Qué hice:** (1) Desarrollado script `scratch/rosalind_warmup.py` implementando `transcribe_dna()`, `reverse_complement()` con `maketrans` y slicing `[::-1]`, `calculate_gc()` y `hamming_distance()` con `zip()`. (2) Verificada salida del script en AlmaLinux WSL. (3) Aplicada protección contra escritura en datasets crudos con `chmod -R a-w data/raw/`. (4) Auditado comportamiento del sistema de archivos mediante intento de sobreescritura con `echo "test" >> data/raw/ecoli_test.fasta` (bloqueado exitosamente con `Permission denied`). (5) Realizado entrenamiento oral de 15 min en inglés técnico (Voz IA) sobre Phred scores, FASTQ headers y GC-bias.
- **💻 Comandos Útiles del Día (Recetario CLI & Python):**
  ```bash
  # 1. Probar complemento reverso en stream Unix (rev + tr)
  echo "GATTACA" | tr 'ATCG' 'TAGC' | rev

  # 2. Ejecutar script Python de Rosalind
  python scratch/rosalind_warmup.py

  # 3. Quitar permisos de escritura a datasets crudos
  chmod -R a-w data/raw/

  # 4. Inspeccionar permisos octales/simbólicos (-r--r--r--)
  ls -l data/raw/

  # 5. Forzar adición a Git de scripts en carpeta scratch (ignorada por defecto en .gitignore)
  git add -f scratch/rosalind_warmup.py
  ```
- **📚 Qué aprendí:**
  1. **Complemento Reverso Dual:** En Python, `[::-1]` invierte el string por slicing de paso negativo y `str.maketrans("ATCG", "TAGC")` mapea los pares de Chargaff. En Unix CLI, `rev` invierte la línea y `tr 'ATCG' 'TAGC'` sustituye caracteres en stream.
  2. **Biología del GC-bias & Hamming:** El contenido %GC define la estabilidad térmica de primarios y el sesgo de amplificación en PCR de Illumina. La distancia Hamming $d_H(S_1, S_2) = \sum [S_1[i] \neq S_2[i]]$ es el fundamento matemático para llamadas de SNPs y tasa de error de lectura.
  3. **Seguridad de Datos en Unix (`chmod`):** Eliminar la bandera de escritura (`-w`) en datos crudos previene la pérdida/corrupción accidental de FASTQs originales de terabytes durante pipelines automatizados.
  4. **Archivos Ignorados por Git (`.gitignore`):** La carpeta `scratch/` está configurada en `.gitignore` para pruebas temporales. Para dar seguimiento formal a un script en `scratch/` se requiere `git add -f`.
- **🩹 Cicatrices (errores):** Intentar añadir `scratch/rosalind_warmup.py` con `git add` estándar arrojó aviso de `.gitignore`. Resuelto forzando el seguimiento con `git add -f`.
- **🔗 Evidencia:** Script `scratch/rosalind_warmup.py` verificado en WSL, protección de `data/raw/` (Permission denied). 145 XP acumulados.
- **➡️ Siguiente paso:** Arrancar Semana 2 Día 1 (`R1-ROSA-03` Traducir ARN a Proteína & Motivos + `R1-XUB-006` Python FASTA & N50).

---

## 📅 2026-08-01 — Ejecución Semana 1 Día 2 (`R1-XUB-004`, `R1-XUB-005` & BioSocial 5)

- **⏱️ Tiempo:** 6.0 h (Práctica deliberada en AlmaLinux WSL)
- **🎯 Objetivo del día:** Inspección de FASTQ comprimidos en stream, depuración de magic bytes de compresión gzip y manipulación fina de registros NGS con `awk` y `grep`.
- **🛠️ Qué hice:** (1) Descargada muestra FASTQ y resuelto error forense de archivo no comprimido en disco (`mv` + `gzip -f`). (2) Inspeccionadas 3 lecturas con `zcat ecoli_reads.fastq.gz | head -n 12` analizando la cabecera `@`, la secuencia y los caracteres ASCII Phred+33 de calidad. (3) Navegado de forma fluida e interactiva con `zless -S`. (4) Extraídas cabeceras (`awk 'NR%4==1'`) y secuencias (`awk 'NR%4==2'`). (5) Filtrado y contado lecturas con motivo `TTGGC` usando `grep` en stream. (6) Calculada longitud en pares de bases (`wc -c` descontando salto de línea `\n`).
- **💻 Comandos Útiles del Día (Recetario CLI):**
  ```bash
  # 1. Renombrar y comprimir correctamente a formato gzip real
  mv ecoli_reads.fastq.gz ecoli_reads.fastq
  gzip -f ecoli_reads.fastq
  file ecoli_reads.fastq.gz

  # 2. Inspeccionar primeras 12 líneas (3 lecturas de 4 líneas cada una)
  zcat ecoli_reads.fastq.gz | head -n 12

  # 3. Navegación interactiva fluida sin desajustar líneas (presionar 'q' para salir)
  zless -S ecoli_reads.fastq.gz

  # 4. Contar líneas totales del archivo comprimido
  zcat ecoli_reads.fastq.gz | wc -l

  # 5. Extraer únicamente los IDs/headers (Línea 1 de cada ciclo de 4)
  zcat ecoli_reads.fastq.gz | awk 'NR%4==1'

  # 6. Extraer únicamente las secuencias de ADN (Línea 2 de cada ciclo de 4)
  zcat ecoli_reads.fastq.gz | awk 'NR%4==2'

  # 7. Buscar un motivo específico ("TTGGC") dentro de las secuencias de ADN
  zcat ecoli_reads.fastq.gz | awk 'NR%4==2' | grep "TTGGC"

  # 8. Contar cuántas lecturas contienen dicho motivo
  zcat ecoli_reads.fastq.gz | awk 'NR%4==2' | grep "TTGGC" | wc -l

  # 9. Calcular la longitud de bases de la primera lectura (26 bytes menos 1 salto de línea = 25 bp)
  zcat ecoli_reads.fastq.gz | awk 'NR%4==2' | head -n 1 | wc -c
  ```
- **📚 Qué aprendí:** 
  1. **Anatomía del FASTQ & Phred+33:** El estándar FASTQ es un ciclo estricto de 4 líneas (1: `@Header`, 2: Secuencia, 3: `+`, 4: Calidad ASCII). La calidad Phred se calcula como $Q = -10 \log_{10}(P_{\text{error}})$. Para almacenarlo en un solo byte se suma el offset 33 de ASCII ($Q = \text{ASCII} - 33$). Por ejemplo, el símbolo `;` (ASCII 59) representa $Q = 26$ (99.75% de certeza).
  2. **Streaming & Buffers en Unix:** La tubería `|` transfiere bytes directamente del búfer `stdout` de `zcat` al `stdin` de `head`/`awk`. Al recibir `head` las líneas solicitadas, envía una señal `SIGPIPE` que detiene la descompresión sin saturar RAM ni I/O de disco.
  3. **Magic Bytes & Diagnósticos CLI:** La extensión de archivo es cosmética en Linux. `zcat` exige los bytes mágicos `1f 8b`. Archivos descargados en texto plano con extensión `.gz` causan el fallo `not in gzip format`, resolviéndose comprimiendo con `gzip -f`.
  4. **Procesamiento Cíclico en `awk`:** El operador módulo `NR%4` permite aislar componentes específicos del FASTQ (`NR%4==1` para headers, `NR%4==2` para secuencias, `NR%4==0` para calidades).
  5. **Arquitectura Multi-OS & Métrica Dual:** El proyecto usa una arquitectura Dual-Clone (Windows host en `C:\Users\...` para IDE/gobernanza y WSL ext4 en `/home/admin/empleoBio` para ejecución de comandos). La métrica **85 XP** representa Puntos de Rango técnico de La Campaña (no dólares USD del Taller MVP).
  6. **Alias de Bash & Entornos de Conda:** Los alias persistentes se configuran en `~/.bashrc` (ej. `alias sync-repo='cd ~/empleoBio && git pull origin main'`). Al ejecutar `source ~/.bashrc`, el shell recarga el perfil reiniciando la pila de Conda al entorno `(base)`. Para volver a las librerías bioinformáticas se ejecuta `conda activate bio-env`. Los alias y Git funcionan independientemente de cuál entorno virtual de Python esté activo.
- **🩹 Cicatrices (errores):** 
  - `gzip: not in gzip format` debido a descarga de texto plano nombrada como `.gz` por `curl`. Corregido renombrando a `.fastq` y aplicando `gzip -f`.
  - Confusión sobre sincronización entre clones de Windows y WSL. Resuelto documentando en `AGENTS.md` el flujo `git push` (Windows/WSL) $\rightarrow$ `git pull` (WSL/Windows).
- **🔗 Evidencia:** Archivo `data/raw/ecoli_reads.fastq.gz` verificado en WSL, script de alias `sync`, commits `8236e11` y `3d5a31c`. 85 XP acumulados.
- **➡️ Siguiente paso:** Arrancar Semana 1 Día 3 (`R1-ROSA-01` ADN/ARN + `R1-ROSA-02` GC & Hamming + `R1-XUB-010` Permisos chmod).

---
